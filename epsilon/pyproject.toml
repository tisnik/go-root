[project]
name = "epsilon"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "transcrypt>=3.9.5",
]

[tool.uv.workspace]
members = [
    "epsilon",
]
